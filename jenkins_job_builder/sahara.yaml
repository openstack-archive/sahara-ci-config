- job:
    name: 'gate-saharaclient-tempest-tests'
    defaults: global
    concurrent: true
    builders:
      - gerrit-git-prep
      - shell: "rm -rf sahara-ci-config\
              \ngit clone https://github.com/stackforge/sahara-ci-config /tmp/sahara-ci-config\
              \nFUNCTION_PATH=/tmp/sahara-ci-config/slave-scripts/functions.sh bash -x /tmp/sahara-ci-config/slave-scripts/gate-saharaclient-integration.sh"
    properties:
      - zeromq-event
    wrappers:
      - timeout:
          timeout: 120
          fail: true
      - timestamps
    publishers:
      - console-log
      - trigger-parameterized-builds:
          - project: "integration-cleanup"
            predefined-parameters:
               "PREV_BUILD=$BUILD_NUMBER-$ZUUL_CHANGE-$ZUUL_PATCHSET\
               \nPREV_JOB=$JOB_NAME\
               \nHOST_NAME=$NODE_NAME"
    node: precise-neutron || precise-nova-network

- job-template:
    name: 'gate-saharaclient-integration-{plugin-neutron}'
    defaults: global
    concurrent: true
    builders:
      - gerrit-git-prep
      - shell: "rm -rf sahara-ci-config\
              \ngit clone https://github.com/stackforge/sahara-ci-config /tmp/sahara-ci-config\
              \nFUNCTION_PATH=/tmp/sahara-ci-config/slave-scripts/functions.sh bash -x /tmp/sahara-ci-config/slave-scripts/gate-saharaclient-integration.sh"
    properties:
      - zeromq-event
    wrappers:
      - timeout:
          timeout: 120
          fail: true
      - timestamps
    publishers:
      - console-log
      - trigger-parameterized-builds:
          - project: "integration-cleanup"
            predefined-parameters:
               "PREV_BUILD=$BUILD_NUMBER-$ZUUL_CHANGE-$ZUUL_PATCHSET\
               \nPREV_JOB=$JOB_NAME\
               \nHOST_NAME=$NODE_NAME"
    node: 'precise-neutron'

- job-template:
    name: 'gate-saharaclient-integration-{plugin-nova_network}'
    defaults: global
    concurrent: true
    builders:
      - gerrit-git-prep
      - shell: "rm -rf sahara-ci-config\
              \ngit clone https://github.com/stackforge/sahara-ci-config /tmp/sahara-ci-config\
              \nFUNCTION_PATH=/tmp/sahara-ci-config/slave-scripts/functions.sh bash -x /tmp/sahara-ci-config/slave-scripts/gate-saharaclient-integration.sh"
    properties:
      - zeromq-event
    wrappers:
      - timeout:
          timeout: 120
          fail: true
      - timestamps
    publishers:
      - console-log
      - trigger-parameterized-builds:
          - project: "integration-cleanup"
            predefined-parameters:
               "PREV_BUILD=$BUILD_NUMBER-$ZUUL_CHANGE-$ZUUL_PATCHSET\
               \nPREV_JOB=$JOB_NAME\
               \nHOST_NAME=$NODE_NAME"
    node: 'precise-nova-network'

- job-template:
    name: 'gate-sahara-integration-{plugin-neutron}'
    defaults: global
    concurrent: true
    builders:
      - gerrit-git-prep
      - shell: "rm -rf sahara-ci-config\
              \ngit clone https://github.com/stackforge/sahara-ci-config\
              \nFUNCTION_PATH=sahara-ci-config/slave-scripts/functions.sh bash -x sahara-ci-config/slave-scripts/gate-sahara-integration.sh"
    properties:
      - zeromq-event
    wrappers:
      - timeout:
          timeout: 120
          fail: true
      - timestamps
    publishers:
      - console-log
      - trigger-parameterized-builds:
          - project: "integration-cleanup"
            predefined-parameters:
               "PREV_BUILD=$BUILD_NUMBER-$ZUUL_CHANGE-$ZUUL_PATCHSET\
               \nPREV_JOB=$JOB_NAME
               \nHOST_NAME=$NODE_NAME"
    node: 'precise-neutron'

- job-template:
    name: 'gate-sahara-integration-{plugin-nova_network}'
    defaults: global
    concurrent: true
    builders:
      - gerrit-git-prep
      - shell: "rm -rf sahara-ci-config\
              \ngit clone https://github.com/stackforge/sahara-ci-config\
              \nFUNCTION_PATH=sahara-ci-config/slave-scripts/functions.sh bash -x sahara-ci-config/slave-scripts/gate-sahara-integration.sh"
    properties:
      - zeromq-event
    wrappers:
      - timeout:
          timeout: 120
          fail: true
      - timestamps
    publishers:
      - console-log
      - trigger-parameterized-builds:
          - project: "integration-cleanup"
            predefined-parameters:
               "PREV_BUILD=$BUILD_NUMBER-$ZUUL_CHANGE-$ZUUL_PATCHSET\
               \nPREV_JOB=$JOB_NAME
               \nHOST_NAME=$NODE_NAME"
    node: 'precise-nova-network'

- project:
    name: sahara
    github-org: openstack
    plugin-neutron:
     - vanilla-1
     - vanilla-2.3
     - vanilla-2.4
     - spark
     - transient
     - heat_transient
    plugin-nova_network:
     - cdh-ubuntu
     - cdh-centos
     - hdp1
     - hdp2
    jobs:
     - 'gate-sahara-integration-{plugin-neutron}'
     - 'gate-sahara-integration-{plugin-nova_network}'
     - 'gate-saharaclient-integration-{plugin-neutron}'
     - 'gate-saharaclient-integration-{plugin-nova_network}'

- job:
    name: integration-cleanup
    project-type: freestyle
    defaults: global
    description: "<b>This job is managed by Jenkins Job Builder, do not edit it through WebUI.\
                  \n<p>Please use this repository to make changes: <a href=\"https://github.com/stackforge/sahara-ci-config\">https://github.com/stackforge/sahara-ci-config</a></b>\
                 \n<p>This job destroys vms which were not deleted after integration tests"
    disabled: false
    concurrent: false
    node: 'master'

    parameters:
      - string:
          name: PREV_BUILD
          default: default
          description:
      - string:
          name: PREV_JOB
          default:
          description:
      - string:
          name: HOST_NAME
          default:
          description:

    builders:
    - shell: "rm -rf sahara-ci-config\
            \ngit clone https://github.com/stackforge/sahara-ci-config\
            \nbash sahara-ci-config/slave-scripts/integration-cleanup.sh"

- job:
    name: gate-sahara-pep8-trunk
    project-type: freestyle
    defaults: global
    description: "<b>This job is managed by Jenkins Job Builder, do not edit it through WebUI.\
                  \n<p>Please use this repository to make changes: <a href=\"https://github.com/stackforge/sahara-ci-config\">https://github.com/stackforge/sahara-ci-config</a></b>\
                 \n<p>This job runs pep8 check using trunk version of hacking"
    disabled: false
    concurrent: true
    node: precise-neutron || precise-nova-network

    builders:
    - gerrit-git-prep
    - shell: "rm -rf sahara-ci-config\
              \ngit clone https://github.com/stackforge/sahara-ci-config\
              \nbash -x sahara-ci-config/slave-scripts/gate-sahara-pep8-trunk.sh"
    properties:
      - zeromq-event
    publishers:
      - console-log
